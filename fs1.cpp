#include <fstream>
#include <iostream>
using namespace std;

int main()
{
        string storeName;

    // // to create and write file
    // ofstream CreateWriteFile("test.txt");

    // // .write file
    // CreateWriteFile << "this is another file that  \n we have created for practice";

    // CreateWriteFile.close();

    // CreateWriteFile << "this is after closing the file \n we have created for practice";

    // // read file
    // ifstream toReadFile("test.txt");

    // while (getline(toReadFile, storeName))
    // {
    //     cout << toReadFile;
    // }
    // return 0;
}